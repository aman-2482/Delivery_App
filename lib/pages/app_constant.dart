/// Small constants used by the app. Keep sensitive keys out of source control.
/// This file provides keys used by the app. Do NOT commit real secret
/// keys to source control. Use environment variables or secure storage.

const String publishableKey = '';

/// Your Stripe secret key used server-side. Leave empty in source control.
const String secretKey = '';
